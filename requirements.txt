# ============================================================
# CrashDefNet — requirements.txt (CPU)
# ============================================================
#
# Installation:
#   pip install -r requirements.txt
#
# GPU (CUDA 12.6):
#   pip install -r requirements-gpu.txt
# ============================================================

# ── PyTorch (CPU) ────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.6,<3.0

# ── PyTorch Geometric ────────────────────────────────────────
torch-geometric>=2.5,<3.0

# ── Data ─────────────────────────────────────────────────────
tfrecord            # reads the DeepMind raw data (*.tfrecord) during export
numpy>=1.24,<3.0

# ── Configuration / utilities ────────────────────────────────
toml>=0.10
tqdm>=4.60

# ── Optional: .vtu export of the rollout (predict/rollout.py) ─
# Without pyvista the rollout runs but writes no VTU files.
pyvista>=0.43,<1.0

# Optional: loss curves and paper comparison (utils/plot_losses.py,
# utils/compare_paper.py)
matplotlib>=3.7
